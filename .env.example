# ============================================================
# NodePress - Environment Variables Example
# WordPress-compatible CMS built with Node.js / TypeScript
# ============================================================
# Copy this file to .env and adjust values for your environment
#   cp .env.example .env
# ============================================================

# Database - PostgreSQL (via PgBouncer)
# DATABASE_URL: Connection through PgBouncer (port 6432)
# DATABASE_DIRECT_URL: Direct connection (for migrations, port 5432)
DATABASE_URL=postgresql://nodepress:nodepress@localhost:6432/nodepress
DATABASE_DIRECT_URL=postgresql://nodepress:nodepress@localhost:5432/nodepress

# Redis - Cache & Queue (BullMQ)
REDIS_URL=redis://localhost:6379

# S3 / MinIO - Media Storage
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=nodepress
S3_SECRET_KEY=nodepress123
S3_BUCKET=nodepress-media
S3_PUBLIC_URL=http://localhost:9000/nodepress-media

# Authentication & JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# Security Keys & Salts (Gap F-01)
# Used for cookie encryption, nonce generation, and
# cryptographic operations. Generate unique values per
# installation using: openssl rand -base64 48
AUTH_KEY=change-me
AUTH_SALT=change-me
SECURE_AUTH_KEY=change-me
SECURE_AUTH_SALT=change-me
LOGGED_IN_KEY=change-me
LOGGED_IN_SALT=change-me
NONCE_KEY=change-me
NONCE_SALT=change-me
SECRET_KEY=change-me

# Application URLs
APP_URL=http://localhost:3000
API_URL=http://localhost:3001
NODE_ENV=development
NODEPRESS_DEBUG=true

# Cross-Origin Resource Sharing (Gap F-08)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Maintenance Mode (Gap F-14)
# When enabled, returns 503 for all non-admin requests.
# MAINTENANCE_ALLOWED_IPS: Comma-separated IPs that bypass
MAINTENANCE_MODE=false
MAINTENANCE_ALLOWED_IPS=

# Logging
# LOG_LEVEL: debug | info | warn | error
LOG_LEVEL=debug
LOG_FILE=storage/logs/nodepress.log

# Admin Panel (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Email (Optional - for notifications)
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# EMAIL_FROM=noreply@nodepress.local

# Rate Limiting (Redis-backed)
# RATE_LIMIT_TTL=60
# RATE_LIMIT_MAX=100
# RATE_LIMIT_LOGIN_MAX=5

# Plugin System
# LOCKED_PLUGINS=seo,cache
# DISABLE_PLUGIN_AUTO_UPDATE=false
# PLUGIN_SANDBOX_MEMORY_LIMIT=128
# PLUGIN_SANDBOX_TIMEOUT=5000

# 2FA (Recommended for Admin roles in production)
# TOTP_ISSUER=NodePress
# TOTP_ENFORCE_FOR_ROLES=ADMIN,SUPER_ADMIN

# Observability (Optional)
# SENTRY_DSN=
# OTEL_EXPORTER_OTLP_ENDPOINT=
# METRICS_ENABLED=false
