# NodePress .npmrc — Compatible with npm AND pnpm

# Suppress funding messages (cleaner install output)
fund=false

# Set audit level to only show high+ (suppress moderate/low noise)
audit-level=high

# Save exact versions instead of ranges
save-exact=true
